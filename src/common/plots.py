# Shared plotting utilities.
# Add matplotlib/plotly chart helpers here as needs grow.
# Keep functions generic and parameterized — project-specific
# chart logic stays in the project's own notebooks/outputs.
